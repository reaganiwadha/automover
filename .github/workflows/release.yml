# .github/workflows/release.yaml

on:
  push :
    branches:
      - trunk
  release:
    types: [created]

permissions:
  contents: write
  packages: write

jobs:
  release-linux-amd64:
    name: release linux/amd64
    runs-on: ubuntu-latest
    steps:
      - uses: wangyoucao577/go-release-action@v1
        env:
          CC: x86_64-w64-mingw32-gcc
          CGO_ENABLED: 1
        with:
          pre_command: apt-get install -y gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64
          github_token: ${{ secrets.GITHUB_TOKEN }}
          goos: windows
          goarch: amd64
